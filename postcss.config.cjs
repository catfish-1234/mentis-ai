/**
 * PostCSS Configuration — MentisAI
 *
 * Registers Tailwind CSS and Autoprefixer as PostCSS plugins.
 * Autoprefixer automatically adds vendor prefixes for cross-browser
 * CSS compatibility.
 */
module.exports = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
}
